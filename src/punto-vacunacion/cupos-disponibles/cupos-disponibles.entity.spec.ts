import { CuposDisponiblesEntity } from './cupos-disponibles.entity';

describe('CuposDisponiblesEntity', () => {
  it('should be defined', () => {
    expect(new CuposDisponiblesEntity()).toBeDefined();
  });
});
