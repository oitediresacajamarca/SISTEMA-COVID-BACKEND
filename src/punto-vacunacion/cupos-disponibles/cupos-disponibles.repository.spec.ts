import { CuposDisponiblesRepository } from './cupos-disponibles.repository';

describe('CuposDisponiblesRepository', () => {
  it('should be defined', () => {
    expect(new CuposDisponiblesRepository()).toBeDefined();
  });
});
