import { PuntoProgramacionEntity } from './punto-programacion.entity';

describe('PuntoProgramacionEntity', () => {
  it('should be defined', () => {
    expect(new PuntoProgramacionEntity()).toBeDefined();
  });
});
