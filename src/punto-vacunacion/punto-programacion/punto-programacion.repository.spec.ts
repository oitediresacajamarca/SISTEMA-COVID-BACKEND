import { PuntoProgramacionRepository } from './punto-programacion.repository';

describe('PuntoProgramacionRepository', () => {
  it('should be defined', () => {
    expect(new PuntoProgramacionRepository()).toBeDefined();
  });
});
