import { PuntoVacunacionRepository } from './punto-vacunacion.repository';

describe('PuntoVacunacionRepository', () => {
  it('should be defined', () => {
    expect(new PuntoVacunacionRepository()).toBeDefined();
  });
});
