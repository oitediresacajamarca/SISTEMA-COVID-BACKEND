import { EquipoCovidRepository } from './equipo-covid.repository';

describe('EquipoCovidRepository', () => {
  it('should be defined', () => {
    expect(new EquipoCovidRepository()).toBeDefined();
  });
});
