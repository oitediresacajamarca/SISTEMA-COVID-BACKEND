import { VacunacionCitaRepository } from './vacunacion-cita.repository';

describe('VacunacionCitaRepository', () => {
  it('should be defined', () => {
    expect(new VacunacionCitaRepository()).toBeDefined();
  });
});
