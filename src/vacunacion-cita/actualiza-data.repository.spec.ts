import { ActualizaDataRepository } from './actualiza-data.repository';

describe('ActualizaDataRepository', () => {
  it('should be defined', () => {
    expect(new ActualizaDataRepository()).toBeDefined();
  });
});
